spring:
  application:
    name: user-service

  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: jdbc:postgresql://localhost:8889/bankdb

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create


mock:
  service:
    json-placeholder:
      url: https://jsonplaceholder.typicode.com/posts/dev
      connection-timeout: 5s
      read-timeout: 10s
      a:
        b: aaa
        c: 12
      test:
        - 1
        - 2
        - 3
        - 4
        - 5
      map:
        key1: value1
        key2: value2
        key3: value3

